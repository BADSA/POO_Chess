package cr.tec.programada.poo.servlets;

import javax.servlet.http.HttpServletRequest;
import javax.servlet.http.HttpServletResponse;

//import cr.tec.ejemplos.poo.modelo.Numero;

public class ComandoIniciarJuego implements ComandoAplicacionWeb {

	@Override
	public void ejecutar(HttpServletRequest request,
			HttpServletResponse response) {
		//int n = Integer.parseInt(request.getParameter("num"));
		//Numero num = new Numero(n);
		//request.setAttribute("num", n);
		//request.setAttribute("resultado", num.obtenerCuadrado());
	}

}
